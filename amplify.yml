# touch again and again and again and again and again and again
version: 1
backend:
  phases:
    preBuild:
      commands:
        - nvm install 14
        - nvm use 14
        - npm install -g @aws-amplify/cli@12.12.7-hotfix-rc-20241004-attempt-3.0
    build:
      commands:
        - amplify -v
        - amplifyPush --simple
frontend:
  phases:
    build:
      commands:
        - echo "true"
  artifacts:
    baseDirectory: src
    files:
      - "**/*"
